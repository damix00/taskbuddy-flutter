import 'package:flutter/material.dart';

class AppOutdated extends StatelessWidget {
  const AppOutdated({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('app outdated');
  }
}