import 'package:flutter/material.dart';

class OutgoingChats extends StatelessWidget {
  const OutgoingChats({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Outgoing Chats'),
    );
  }
}