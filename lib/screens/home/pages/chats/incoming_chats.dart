import 'package:flutter/material.dart';

class IncomingChats extends StatelessWidget {
  const IncomingChats({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Incoming Chats'),
    );
  }
}